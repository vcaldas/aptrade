[project]
name = "aptrade"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "backtesting>=0.6.5",
    "ipykernel>=6.30.1",
    "polygon>=1.2.6",
    "polygon-api-client>=1.15.3",
    "python-dotenv>=1.1.1",
]

[project.scripts]
hello = "aptrade:main.main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = ["aptrade"]
